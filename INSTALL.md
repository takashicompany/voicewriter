# Voicewriter インストールガイド

このドキュメントは、`Voicewriter-vX.Y.Z.zip` を受け取ったユーザー向けの導入手順です。Voicewriterは現時点でApple Developer Programに加入しておらず、Appleの公証(notarization)を受けていません。そのため初回起動時にmacOSのGatekeeperによる警告が表示されますが、これは想定内の挙動です。

## セキュリティに関する注意

**信頼できる配布元(受け取った本人が経緯を把握しているルート)から入手したファイルのみを実行してください。** 公証を受けていないアプリはAppleによる事前のマルウェア審査を経ていないため、素性の分からない相手から渡された`.zip`や`.app`は開かないでください。

配布ファイルのSHA-256を、配布者から別途(チャット等)で受け取ったハッシュ値と突き合わせることを推奨します。

```sh
shasum -a 256 Voicewriter-vX.Y.Z.zip
```

## 1. インストール

1. `Voicewriter-vX.Y.Z.zip` をダブルクリックして展開し、`Voicewriter.app` を取り出します(**解凍はFinderのダブルクリックを推奨**。ターミナルの`unzip`コマンドや他の解凍ツールでも展開できますが、Finderでの展開が最も確実です)。
2. `Voicewriter.app` を `/Applications` フォルダにドラッグ&ドロップ、またはFinderでコピーします。

## 2. 初回起動(Gatekeeperの操作)

自己署名アプリのため、初回起動時にmacOSが警告を出します。

1. `/Applications/Voicewriter.app` をダブルクリックで起動しようとすると、「開発元が未確認のため開けません」といった趣旨のダイアログが表示されます。いったん「OK」で閉じます。
2. **システム設定 > プライバシーとセキュリティ** を開きます。
3. 画面下の方に「"Voicewriter"は開発元を確認できないため、使用がブロックされました」という表示があるので、その横の**「このまま開く」**をクリックします。
4. 管理者パスワード(またはTouch ID)の入力を求められるので入力します。
5. 再度確認ダイアログが出るので「開く」を選ぶとアプリが起動します。

以降は通常どおりダブルクリックで起動できます(この操作はアプリごとに初回1回だけ必要です)。

### 「"Voicewriter"は壊れているため開けません」と表示された場合

ダウンロード経路(ブラウザや圧縮解凍ツール)によっては、Gatekeeperが自己署名アプリに対してこのように「壊れている」と報告することがあります。多くの場合、実体はmacOSがダウンロード由来のファイルに付与する隔離属性(quarantine)によるもので、ターミナルで以下を実行し隔離属性を除去すれば解消します。ただし、入手経路に心当たりが無い場合や、以下を実行しても解消しない場合は、本当にファイルが破損している・署名が壊れている可能性もあるため、その場合は配布元に再配布を依頼し、むやみに実行しないでください。

```sh
xattr -dr com.apple.quarantine /Applications/Voicewriter.app
```

実行後、上記「2. 初回起動」の手順から再度お試しください。

## 3. 権限の許可

Voicewriterはメニューバー常駐アプリで、音声入力とカーソル位置へのテキスト自動挿入のために2つの権限を使います。いずれも初回利用時にmacOSのダイアログで許可を求められます。

### マイクへのアクセス

アプリ起動時に、マイクへのアクセス許可ダイアログが表示されます。「許可」を選択してください。後から変更する場合は **システム設定 > プライバシーとセキュリティ > マイク** で `Voicewriter` を確認・切り替えできます。

### アクセシビリティ(カーソル位置へのテキスト挿入に必要)

文字起こし結果をカーソル位置に自動挿入する機能(クリップボード経由でCmd+Vを合成する方式)のため、アクセシビリティの許可が必要です。

1. **システム設定 > プライバシーとセキュリティ > アクセシビリティ** を開きます。
2. リストに `Voicewriter` が表示されるので、トグルをオンにします(表示されない場合は、Voicewriterでテキスト挿入を一度試みると自動的にリストへ追加されます)。
3. 管理者パスワードの入力を求められる場合があります。

この許可は、同じ署名で再ビルド・アップデートされたアプリであれば維持されます。アプリを完全に別のビルド(異なる署名)に入れ替えた場合は、許可が外れて再度オンにする必要があることがあります。

## 4. 初回起動時のモデルの自動セットアップ(約1.6GB、Wi-Fi推奨)

Voicewriterは音声認識(STT)エンジンとしてwhisper.cpp(モデル: `ggml-large-v3-turbo`、約1.6GB)を使用しますが、**アプリ本体にはモデルを同梱していません**。特別な操作は不要です。**初回起動時に自動でセットアップされます**。

1. 初回起動直後、画面下部にダウンロード進捗を示すHUD(「初回セットアップ中: モデルをダウンロードしています… NN%」)が表示されます。この間もアプリは通常どおり常駐し続け、完了を待つ必要はありません。
2. ダウンロード完了後は自動的にモデルが読み込まれ、以降は再ダウンロード不要です。セットアップ中に音声入力(F13等)を行おうとした場合は、ダミーの文字起こしが挿入されることを防ぐため「セットアップ中です」と表示され、録音は行われません。完了後は通常どおり利用できます。

ダウンロードには数分〜(回線速度によっては)数十分かかる場合があるため、可能であればWi-Fi環境での初回起動を推奨します。ディスクの空き容量は2.5GB以上を確保してください。

ディスク容量不足やネットワーク接続失敗などでダウンロードに失敗した場合は、HUDとメニューバーにその旨と再試行方法が表示されます。その場合はメニューバーの「設定...」>「音声認識」タブから手動で再試行できます(ターミナルで`scripts/download-model.sh`を実行して手動配置することも可能です)。

なお、発話区間検出(VAD)用の補助モデル(約860KB)も、初回起動時にバックグラウンドで自動ダウンロードされます(こちらは失敗してもアプリの起動や他機能には影響しません)。

## 5. LLM整形機能を使う場合(任意・Ollamaのセットアップ)

Voicewriterには、音声認識結果をローカルLLMで整形(誤字修正・フィラー除去等)するオプション機能があります。**この機能は必須ではなく、セットアップしなくてもアプリは正常に動作します**(整形が使えない場合はwhisper.cppの生の認識結果がそのまま挿入されます)。

整形機能を使いたい場合は、以下を用意してください。

1. [Ollama](https://ollama.com/) をインストールし、ローカルで起動しておく(既定では `http://localhost:11434` で待ち受け)。
2. 整形に使うモデルを取得する。例:

   ```sh
   ollama pull qwen3:14b
   ```

   (`qwen3:14b` はVoicewriterの既定モデルです。マシンのスペックに応じて設定画面から別モデルに切り替えることもできます。)
3. Voicewriterの設定画面「整形」タブで機能をオンにします。

Ollamaが起動していない・応答がない場合は自動的に整形をスキップして生の認識結果を使うため、Ollamaを用意しなくてもVoicewriter自体の利用に支障はありません。この場合、メニューバーに「LLM整形: 無効(Ollama未検出)」と静かに表示されるだけで、警告ダイアログや繰り返しの通知は出ません。後からOllamaを起動すれば、次回の音声入力から自動的に整形が有効になります(アプリの再起動は不要です)。

## 6. アンインストール

`/Applications/Voicewriter.app` をゴミ箱に入れるだけで削除できます。ダウンロード済みのモデルファイル等を含む設定・キャッシュを完全に削除したい場合は、以下も併せて削除してください。

```sh
rm -rf ~/Library/Application\ Support/Voicewriter
defaults delete dev.voicewriter.app 2>/dev/null || true
```
